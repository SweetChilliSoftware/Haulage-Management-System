spring:
  profiles:
    active: development
    
--------------------------------

spring:
  profiles: development
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SweetChilliSoftware/Haulage-Management-Microservices-Config
        health:
          
server:
  port: 9000
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false
eureka:
  instance:
    hostname: config-service
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://eureka-service:8761/eureka/
